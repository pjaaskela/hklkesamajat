---
deployment:
  tasks:
    - export DEPLOYPATH=/home/hklkesam/public_html/
    - /bin/cp -f *.html $DEPLOYPATH
    - /bin/cp -f *.css $DEPLOYPATH
    - /bin/cp -f *.js $DEPLOYPATH
    - /bin/cp -f *.jpg $DEPLOYPATH
    - /bin/cp -f *.pdf $DEPLOYPATH
